"""devctl — generic Docker-Compose dev/test helper CLI.

Entry point: `devctl` console script (see pyproject.toml).
Captured command output + run manifests so AI agents get structured,
re-readable logs instead of truncated terminal scrollback.
"""
__version__ = "0.1.0"
