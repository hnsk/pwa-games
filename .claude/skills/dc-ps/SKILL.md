---
name: dc-ps
description: Captured `docker compose ps -a` — snapshot of stack/container status to a re-readable log.
---

# dc-ps

```
tools/scripts/ps
```

`docker compose ps -a`, wrapped in capture. Read the `LOG=` path for the
status table.
